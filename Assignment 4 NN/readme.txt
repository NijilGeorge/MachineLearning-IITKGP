1. The input to the problem statement is downloaded from the link provided and is saved as 'Assignment_4_data.txt'.
2. The code for the first part is present in p1.py. To run it, execute the command
        python p1.py 
    in the terminal. 
3. The code for the second part is present in p2.py. To run it, execute the command
        python p2.py <layer-1> <layer-2> 
    in the terminal. The values <layer-1> and <layer-2> correspond to the number of neurons in the first and second hidden layers, taken as command line arguments
4. The required libraries for these codes are -- numpy, matplotlib (for plotting), nltk (for removing stopwords)